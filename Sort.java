import java.util.ArrayList;
public class Sort {
	/*
	public static ArrayList<T> heapSort (ArrayList<T> arreglo){
		private Heap monticulo; 
	}
	*/

	public static ArrayList<Integer> bubbleSort (ArrayList<Integer> arreglo){
		/*
		for (int i=1 ; i<arreglo.size() ; i++ ) {
			for (int j=0; i<arreglo.size()-i ; j++) {
				if (0<(arreglo.get(j)).compareTo(arreglo.get(j+1))) {
					Integer aux = arreglo.get(j);
					arreglo.set(j,arreglo.get(j+1));
					arreglo.set(j+1,aux);
				}
			}
		}
		*/
		return arreglo;
	}
}
